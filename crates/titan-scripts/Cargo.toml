[package]
name = "titan-scripts"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Lua script engine (mlua) for host-side automation."

[dependencies]
mlua = { version = "0.11", features = ["lua54", "vendored"] }
thiserror = "2.0"
