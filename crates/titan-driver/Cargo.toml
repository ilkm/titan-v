[package]
name = "titan-driver"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Kernel / VMBus boundary traits (Phase 4); no in-tree kernel driver."

[dependencies]
thiserror = "2.0"
