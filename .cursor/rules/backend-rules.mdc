---
description: Titan-v Rust 服务与系统层——异步、错误、unsafe、Hyper-V/Win32、协议、Lua、推流与资源生命周期
globs:
  - "**/*.rs"
alwaysApply: true
---

# Rust 后端与系统层规则

适用于**宿主机客户端**、**中控后台逻辑**、**协议**、**与 Hyper-V / Win32 / 驱动 / VMBus / 编码推流 / Lua 宿主**相关的 Rust 代码。若文件以 **egui 视图拼装与每帧 UI** 为主，同时遵守 `front-rules.mdc`。生成或修改上述职责代码时，须与本文件及 `general.mdc` 一并满足。

## 方法与模块规模（强制）

- **禁止**：单文件多职责（协议、持久化、UI 回调、测试夹具等混在同一 `.rs`）；单函数混杂「协议解析 + 业务状态机 + IO + 日志」；`unsafe` 大块内嵌业务分支；无上限缓存事件或帧；**单 `.rs` 代码行超过 400**；**单个函数/方法代码行超过 30**（自 `fn` 起至该方法对应闭合 `}` 区间内计；口径见 `general.mdc`「Rust 行数硬上限（400 / 30）」）。
- **正确**：**单一职责**：按领域拆 `mod` 与文件，每文件一类协作对象；一函数一事；**单 `.rs` ≤400 代码行**、**单方法 ≤30 代码行**，超出须拆成小函数、子模块或独立文件后再提交；与 UI 解耦：核心逻辑不依赖 `egui::Context`（可通过 channel / 共享状态注入）。

## 异步与并发

- **禁止**：在异步运行时中阻塞 `std::thread::sleep`、长占用互斥锁、无背压无限 `mpsc` 发送。
- **正确**：**tokio**（或项目选定运行时）上 IO 与计时；跨线程用 `Arc`/channels，锁粒度最小；背压：有界通道、丢帧策略或合并更新需文档化。

## 错误类型

- **禁止**：到处 `String` 作为错误类型；混用多种互不转换的错误表示导致调用方只能 `unwrap`。
- **正确**：库边界 **`thiserror`**（或项目约定）；应用顶层 **`anyhow::Result`** 可选；错误上下文用 `.context()`/`.with_context()`；不要将密钥拼进 `Display`。

## `unsafe` 与 FFI

- **禁止**：无注释的 `unsafe`；假设指针永远合法；忽略调用约定与对齐。
- **正确**：每个 `unsafe` 块注明**由谁保证**的不变量；FFI 用 `-sys` crate 或 `windows-rs` 等生成/封装层；最小化 `unsafe` 范围；输入校验在安全封装层完成。

## 虚拟化、驱动与平台

- **禁止**：静默忽略 Hyper-V / WMI / PowerShell 调用失败；差分盘、GPU-PV 等半套配置无状态回滚。
- **正确**：外部命令与 API 有超时、日志与可重试策略；资源创建 **RAII** 或显式 `Drop`/清理路径；失败时尽量释放已分配资源并上报原因。

## 协议与版本

- **禁止**：无版本号的二进制或 JSON 协议；破坏性变更不兼容旧节点。
- **正确**：中控与宿主机协议带 **版本/能力协商**；字段演进可扩展、可拒绝不支持的组合。

## Lua 宿主

- **禁止**：无沙箱边界地 `eval` 任意字符串；脚本错误拖垮宿主进程。
- **正确**：明确 Lua 与 Rust 的权限边界（能调哪些 API）；超时与配额；脚本异常映射为可记录错误，不 panic 整个服务。

## 推流与性能敏感路径

- **禁止**：在编码/采集线程做重逻辑或阻塞磁盘；无界缓冲帧。
- **正确**：与 UI 线程分离；队列有界；丢帧/降码率策略可配置；关键路径分配可接受性有基本说明或 benchmark 钩子。

## 测试

- **禁止**：纯时间依赖、无断言的测试；强依赖真实 GPU/200 台机器才能跑的单测默认进 CI。
- **正确**：核心状态机与协议用 **单元测试**；硬件相关用 **feature gate** 或集成测试目录；必要时 mock IO 边界。
