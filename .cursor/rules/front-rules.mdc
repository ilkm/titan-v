---
description: Titan-v egui 前端——eframe/egui 组织、状态与每帧成本、Texture 视频、交互与可观测性
globs:
  - "**/*.rs"
alwaysApply: true
---

# egui 前端规则

适用于**中控管理端**或**宿主侧管理界面**等以 **`eframe` + `egui`** 实现的 Rust GUI。与 **推流画面**（`need.md`：解码后贴 Texture、输入坐标映射）相关的 UI 代码须特别遵守「视频与交互」一节。生成或修改上述代码时，须与本文件及 `general.mdc` 一并满足；业务状态与虚拟化调用仍须符合 `backend-rules.mdc`。

## 架构与职责

- **禁止**：在 `ui.*` 闭包内发起阻塞网络/磁盘；在 `update` 里堆叠全部领域逻辑。
- **正确**：**UI 层**只负责展示与用户输入；网络、推流、会话用 **异步任务 + channel / 共享 `Arc<...>>`** 与 UI 通信；每帧只做轻量同步与 `egui` 控件调用。

## 状态组织

- **禁止**：大量散落 `static mut`；跨窗口共享可变全局无锁随意写。
- **正确**：应用状态集中在 **`struct App`**（或分层子状态）；跨线程更新用 **`egui::mutex::Mutex`** 或 channel 投递到下一帧处理；单线程 UI 假设不被破坏。

## 每帧成本与响应

- **禁止**：每帧全量重建昂贵控件树、全表扫描 8000 项无虚拟化；每帧解码整段视频在 UI 线程。
- **正确**：大数据用 **`TableBuilder` / 虚拟滚动 / 折叠分组`**；仅脏区或新帧更新 Texture；重活放在后台线程，UI 只拿最新纹理句柄与元数据。

## 视频与交互（对齐 `need.md`）

- **禁止**：无宽高比与延迟说明的硬编码贴图；点击坐标不经过与宿主机约定的同一套映射就下发。
- **正确**：为视频区域预留 **固定或动态 Texture 占位**；解码与渲染路径错误时在 UI 显示可读状态（连接中/失败重试）；**点击 (x,y)** 与 Rust 侧「相对窗口 → VM 内坐标」的契约写在类型或模块注释中，与中控-宿主协议一致。

## 布局与可读性

- **禁止**：魔法数像素散落；中英文混排无间距习惯。
- **正确**：间距、字号用 **常量或 `egui::style`** 统一；关键操作（批量启动/停止）需 **确认对话框** 或二次确认以防误触。

## 可观测性

- **禁止**：操作失败仅 `tracing` 无 UI 提示。
- **正确**：后台错误通过 **`toasts` / 顶部状态条 / 日志面板** 之一暴露给操作员（不含敏感信息）；长时间任务显示进度或不确定「进行中」状态。

## 代码规模（强制，与 `general.mdc` / `backend-rules.mdc` 一致）

- **禁止**：单文件无限增长；单 `update` / `show_*` 堆叠全部 UI 与业务；**单 `.rs` 代码行超过 400**；**单个函数/方法代码行超过 30**（自 `fn` 至对应闭合 `}` 区间内计；口径见 `general.mdc`「Rust 行数硬上限（400 / 30）」）。
- **正确**：**单一职责**：按面板拆 **`mod`** 或 `impl App { fn show_xxx(&mut self, ui: &mut Ui) }`，视图与状态投递分层；**单 `.rs` ≤400 代码行**、**单方法 ≤30 代码行**，超出拆子模块或提取纯函数后再交付。声明式 UI（`ui.horizontal`、长 `match` 等）**不得**靠大面积 `allow` 绕过：通过**抽具名 `fn`**、子模块或拆 `impl` 到多文件满足行数；社区阅读舒适区见 `general.mdc`「社区阅读习惯」，**硬上限不变**。**egui 高 arity**：`rustfmt` 拆开的**多行 `fn` 签名会计入 30 代码行**；优先拆函数或模块内小 `struct` 收参，**`#[rustfmt::skip]` 仅作最后手段**且须注释理由，详见 `general.mdc`「满足 30 行的最佳实践」与「30 行硬上限与 rustfmt 多行签名」。

## 依赖与质量

- **禁止**：随意 `unwrap` 加载字体/图标资源；禁用 `clippy` 而不修。
- **正确**：资源加载失败有降级 UI；提交前 **`cargo fmt` + `cargo clippy -- -D warnings` + `python3 tools/check_fn_code_lines.py`（退出码 0）**。

## 自检 Checklist（新窗口或大改）

- [ ] 是否存在阻塞 IO/网络在 UI 线程？
- [ ] 列表/网格是否会在千级以上卡顿？是否需要虚拟化或分页？
- [ ] 视频纹理更新是否在后台？失败是否有 UI 反馈？
- [ ] 危险操作是否有确认与可撤销或明确影响范围说明？
- [ ] 是否已执行 `python3 tools/check_fn_code_lines.py` 且退出码为 0？
